import 'package:flutter/material.dart';

class Addticketscreen extends StatelessWidget {
  const Addticketscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Add Ticket Screen"),
      ),
    );
  }
}
